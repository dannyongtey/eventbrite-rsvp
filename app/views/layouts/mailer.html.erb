<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
      /* Email styles need to be inline */
      body {
        font-family: "San Francisco", "Helvetica Neue", Arial, Roboto, sans-serif;
      }

      .button {
        border: 1px solid #0166b3;
        
        padding: 10px;
        border-radius: 3px;
        color: #fff;
        text-decoration: none;
      }

      .button.blue{
				background-color: #0166b3;
      }

      .button.red{
				background-color: #990000;
      }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
